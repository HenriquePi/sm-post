---
trigger: always_on
---

Always Use Shadcn

Always get components with the npx shadcn@latest

Do not use radix or other component libraries if avoidable. if we need to use different component libraries, ask permission
